# GAS.md — Google Apps Script 実装・監査ガイド

GAS(Google Apps Script)プロジェクトでの実装・監査時に確認する注意点。
`CLAUDE.md`の一般ルールに加えて、このファイルの内容を適用する。

## 1. 実行環境の制約

- 単純トリガー（`onOpen`等）は実行時間が数秒〜十数秒で打ち切られる。重い処理はインストーラブルトリガーに逃がす
- インストーラブルトリガー・手動実行は最大6分（Workspace版でも同様）で強制終了する。長時間処理は分割し、`PropertiesService`等で進捗を保存して再開できる設計にする
- 同時実行数に上限がある。同じスクリプトへの多重起動（複数トリガー・複数ユーザーからの同時アクセス）でキューが詰まる前提で設計する
- 時間主導トリガーの発火時刻は指定時刻ちょうどではなく数分単位でずれる。時刻依存のロジックを組まない

## 2. API呼び出し・クォータ

- `SpreadsheetApp` / `DriveApp` / `GmailApp` / `UrlFetchApp`等はサービスごとに1日あたりの呼び出し上限がある（アカウント種別で上限が異なる）
- ループ内で`getRange().getValue()`や`setValue()`を逐次呼ぶN+1呼び出しは禁止。`getValues()`でまとめて取得し、配列操作後に`setValues()`で一括反映する
- `UrlFetchApp`は同期呼び出しのみで並列実行不可。多数のリクエストが必要な場合は`UrlFetchApp.fetchAll()`でバッチ化する
- 外部APIのレート制限・タイムアウトに対してリトライ（指数バックオフ）を入れる

## 3. 型・コーディング規約

- GAS(V8ランタイム)はTypeScriptを直接実行できないため、JSDocの`@typedef`と`@param`/`@returns`で型を明記する（`CLAUDE.md`の全体ルールと同一方針）
- `interface`の代わりに`@typedef {Object} Foo`、`enum`の代わりに`Object.freeze({...})`で表現する
- グローバルスコープが単一ファイル空間のように共有される（`.gs`/`.js`ファイルを跨いで関数・変数が衝突しうる）。命名の衝突を避けるため、名前空間オブジェクトやIIFEでの隠蔽を検討する

## 4. エラーハンドリング・ログ

- `try-catch`で握りつぶさず、`Logger.log`または`console.error`に意味のあるメッセージを残す
- Cloud Loggingと連携する場合、`console.log`系はStackdriverに転送されるが`Logger.log`は転送されない点に注意する
- ユーザー影響のあるエラー（Webアプリ・定期実行バッチの失敗）はメール送信や外部通知で検知できるようにする
- `doGet`/`doPost`は開発者がHTTPステータスコードを設定できない（正常時は302リダイレクト後に200、未捕捉例外や権限エラーでもHTMLページが返る）。そのため以下を徹底する
  - 実装側: 全体を`try-catch`で囲み、成否をレスポンスボディに含める（例: `{ ok: false, error: "..." }`をJSONで返す）
  - 呼び出し側: リダイレクトを追従し、ステータスコードではなくボディの`ok`フィールドとContent-Type（HTMLが返ったら異常）で成否を判定する

## 5. 認証・権限

- `appsscript.json`の`oauthScopes`は必要最小限にする。不要なスコープは実行時の承認画面でユーザーの不信感を招く
- `PropertiesService`は`ScriptProperties`（プロジェクト全体で共有・機密情報向け）・`UserProperties`（実行ユーザーごと）・`DocumentProperties`（紐づくドキュメントごと）の用途を区別して使う
- Webアプリ（`doGet`/`doPost`）のデプロイ設定で「実行ユーザー」を「自分」にするか「アクセスユーザー」にするかで、実行時の権限・クォータ消費元が変わる。公開範囲とセットで確認する

## 6. トリガー・非同期処理

- シンプルトリガー（`onOpen`/`onEdit`等）は認可が必要な処理（`UrlFetchApp`や他人のファイルへのアクセス等）を実行できない。必要な場合はインストーラブルトリガーに切り替える
- 複数のトリガーやユーザー操作から同じ処理が同時実行されうる場合は`LockService`で排他制御する
- 時間主導トリガーの登録処理を`onOpen`等で毎回無条件に呼ぶと、実行のたびにトリガーが重複登録される事故になりやすい。登録前に既存トリガーの有無を確認する

## 7. デプロイ・バージョン管理

- clasp運用時、`.claspignore`で`node_modules`やテストコードなど push 対象外を明示する
- Webアプリはコードを変更しただけでは本番URLに反映されない。デプロイの「新バージョン」作成を伴わない変更は反映漏れになる
- `appsscript.json`の`timeZone`・`dependencies`・`exceptionLogging`等の設定変更はレビュー対象にする（実行時の挙動に直結するため）

## 8. パフォーマンス

- 巨大なスプレッドシート・Driveフォルダの全件取得（`getDataRange().getValues()`をフィルタなしで多用する等）は実行時間・メモリを圧迫する。必要な範囲・列に絞る
- 繰り返し参照する値は`CacheService`（最大6時間・サイズ上限あり）でキャッシュし、API呼び出し回数を減らす
- 数千行を超えるデータ処理は分割実行（トリガーでの継続処理）を検討する。`docs/audit-plan.md`のPerformanceカテゴリと同じ観点で監査する

## 9. セキュリティ

- APIキー・トークン等の機密情報をソースコードにハードコードせず、`ScriptProperties`に保存する
- 外部公開する`doGet`/`doPost`は、パラメータの型・値域チェックを行い、想定外の入力でスプレッドシート破壊やなりすましが起きないようにする
- GASの`doGet`/`doPost`はリクエストヘッダー（`Authorization`等）を読み取るAPIを提供していない。認証トークン等の秘密情報はヘッダーではなくPOSTデータ（`e.postData.contents`）に含めて送るよう実装・呼び出し側双方に促す
- `doGet`のクエリパラメータ（`e.parameter`）はURLに残りアクセスログ等に記録されやすいため、秘密情報はGETクエリでなくPOSTボディに含める
- スプレッドシート・Driveファイルの共有設定（リンクを知る全員に編集権限、等）を実装時に確認し、過剰な公開範囲を作らない

## 10. テスト・デバッグとの連携

- GAS APIへの依存（`SpreadsheetApp`等）とビジネスロジックを分離し、ロジック部分を純粋関数として抽出する
- 純粋関数化した部分は`test-gas`スキルでNode.js上のユニットテストに変換できる。設計段階からこの分離を意識する
- GASのデバッガは実行時間制限の影響を受け、ログ反映にも遅延があるため、`console.log`ベースでの調査を基本とする
