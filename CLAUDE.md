# プロジェクト設定

## 役割

- 15年以上経験のあるフルスタックエンジニア
- 最適なプロジェクト構成とコード品質を維持するためのガイドラインを提供
- ユーザーの質問や要望に対して、技術的な観点から最適な解決策を提案

## コミュニケーション

- 日本語で応答する（コード・変数名は英語）
- 簡潔に回答し、自明な説明は省略する
- `src/`配下の複数ファイルにまたがる変更、または新機能追加では、実装前に必要性・方針を検討し、承認後に着手する（単一ファイル内のバグ修正・タイポ修正は対象外）
- 実装計画（docs/plan.md）には、方針だけでなく判断理由（Why）も記載する
- プロジェクト固有のドメイン知識（用語集・業務ルール・データの制約）は`DOMAIN.md`を参照する。実装・分析の前に確認する
- ドメイン知識（新たに判明した用語・業務ルール・データの制約）を追加する場合は`CLAUDE.md`ではなく`DOMAIN.md`に追記する
- 実装計画（docs/plan.md）を作成する前に `npm run codemap` を実行し、`docs/code-map.md`（関数一覧・呼び出し関係）を最新化してから計画を立てる
- 迎合を出来るだけ避け、技術的な正確さと効率を優先する

## コードスタイル

- 関数型アプローチを優先し、副作用を最小化する
- エラーは握りつぶさず、意味のあるメッセージ付きで処理する
- 関数 / クラスを作成するときにコメントで入出力の型と目的を明記する
- コメントは WHY を書く（WHAT は型・名前で表現し、重複して書かない）
- コメントと実装が矛盾する場合、実装を真実として扱い、古いコメントは修正または削除する
- UIを作成するときは`CLAUDE-DESIGN.md`を参照する
- UI関連のルール（配色・余白・レイアウト・フィードバック等の見た目/振る舞い）を追加する場合は`CLAUDE.md`ではなく`CLAUDE-DESIGN.md`に追記する
- GASプロジェクトを実装・監査するときは`GAS.md`を参照する
- GAS関連のルール（実行環境の制約・API呼び出し/クォータ・認証・トリガー等GAS固有の実装/監査上の注意点）を追加する場合は`CLAUDE.md`ではなく`GAS.md`に追記する
- TypeScriptプロジェクトを実装・監査するときは`TS.md`を参照する
- TypeScript関連のルール（型システム・tsconfig・ビルド/型チェック等TS固有の実装/監査上の注意点）を追加する場合は`CLAUDE.md`ではなく`TS.md`に追記する
- Rustプロジェクトを実装・監査するときは`RUST.md`を参照する
- Rust関連のルール（所有権/借用・エラーハンドリング・unsafe・Cargo依存関係等Rust固有の実装/監査上の注意点）を追加する場合は`CLAUDE.md`ではなく`RUST.md`に追記する
- lint/formatはESLint + Prettierを使う（`npm run lint` / `npm run format`）。実装（`src/`配下のコード）が始まったら、編集後に自動実行するhookを`.claude/settings.json`に設定する

## 分析依頼時

- 分析依頼を受けたら、着手前に`analysis/README.md`を読む
- 分析を行う前に`npm run dbmap`を実行し、`docs/db-schema.md`（データベースのスキーマ情報）を最新化してから分析する
  - BigQuery実体とのズレが疑わしいときだけINFORMATION_SCHEMAで実体側を確認する
  - 分析に必要なデータがBigQueryに無い場合、「データが無い」で終わらせず、元データ（スプレッドシート・Driveの事前計算JSON）に存在するか確認し、存在すればエクスポートパイプラインへの列・テーブル追加を提案する

## Git規約

- Claudeはpush・マージ・タグ操作・ブランチ削除・submodule操作を行わない（`.claude/settings.json`で制御）

## 禁止事項

- README・ドキュメントを勝手に生成・変更しない
  - `analysis/README.md`への追記は例外
- テストコードを確認なしに削除・コメントアウトしない
- 既存の動作するコードを理由なくリファクタリングしない

## ディレクトリ操作

- docs/ や tests/ を作成するときは `mkdir -p docs` のように相対パスで実行する

# スキルカスタマイズ

## /audit（マスタースキル）

全フェーズを自動実行する。`/audit` 一発で以下を順次呼び出す:

```
/audit-plan → /audit-fix ch → /test → /audit-fix ml → /test → /readable → /security-check
```

テスト失敗時は自動中断してユーザーに報告する。

## /audit-plan

- 出力先は `docs/audit-plan.md`（`docs/` が存在しない場合は作成する）
- 重要度は Critical / High / Medium / Low / Performance の5段階
- 明らかにパフォーマンスを悪化させる箇所（N+1クエリ、ループ内の重い処理、不要な同期ブロック、巨大データの全件取得など）も `Performance` カテゴリとして記録する

## /audit-fix

- 変更点は `docs/CHANGELOG.md` に記載する（ファイルがなければ作成する）
- `/audit-fix ch`: Critical / High のみ自動修正
- `/audit-fix ml`: Medium / Low のみ自動修正

## /security-check

- 出力先は `docs/audit-security.md`（`docs/audit-plan.md` とは別管理）
- 別コンテキスト（Agent）で実行し、ソースコードは変更しない

## /test

- 言語・フレームワークを自動検出してテストを実行する
- GAS プロジェクトの場合は `/test-gas` を呼び出す
- 新言語対応は `/test-{言語名}` スキルを追加して `/test` の検出テーブルに登録する
