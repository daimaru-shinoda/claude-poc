# DOMAIN.md — プロジェクト固有ドメイン知識

このプロジェクト固有の用語・業務ルール・データの制約を蓄積する。
コードやgit履歴からは読み取れない「なぜこうなっているか」を記録する場所であり、コードから自明な情報（関数の役割・ファイル構成等）は書かない。

実装・分析に着手する前にこのファイルを確認する。新たに判明したドメイン知識は`CLAUDE.md`ではなくこのファイルに追記する。

`docs/plan.md`（個別タスクの実装計画）とは役割が異なる: `docs/plan.md`はタスクごとに書き換わるのに対し、このファイルはタスクを跨いで蓄積される恒久的な知識を扱う。

## 用語集

<!-- 例:
| 用語 | 意味 | 補足 |
|---|---|---|
| 案件 | 受注が確定する前の商談単位 | 「プロジェクト」とは別概念。案件は複数プロジェクトに分割されうる |
-->

| 用語 | 意味 | 補足 |
|---|---|---|

## 業務ルール

<!-- 例:
- 見積金額は税抜で管理する。税込表示はUI層でのみ変換する
  - Why: 過去に税込で保持していた時期があり、税率変更時に再計算が必要になった経緯がある
-->

## データの制約

<!-- 例:
- `users.email`はNULL許容（旧システムからの移行データに未設定レコードが残っている）
  - Why: 2023年以前に作成されたアカウントはメール認証必須ではなかった
-->
