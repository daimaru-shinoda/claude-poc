# RUST.md — Rust 実装・監査ガイド

Rustプロジェクトでの実装・監査時に確認する注意点。
`CLAUDE.md`の一般ルールに加えて、このファイルの内容を適用する。

## 1. 所有権・借用・ライフタイム

- 不要な`clone()`でコンパイラの借用チェックを回避しない。所有権の設計（誰がいつ値を保持するか）で解決できないか先に検討する
- ライフタイム注釈が必要になった場合、構造体の参照保持を見直し、可能なら所有型（`String`/`Vec<T>`等）に寄せて複雑な注釈を避ける
- `Rc<RefCell<T>>`/`Arc<Mutex<T>>`は所有権設計の代替であり乱用しない。実行時パニック（`RefCell`の二重借用）やデッドロック（`Mutex`のロック順序不整合）のリスクを伴う

## 2. エラーハンドリング

- `unwrap()`/`expect()`はプロトタイプ・テストコード限定とし、本番コードパスでは`Result`を伝播させる（`?`演算子を使う）
- アプリケーションコードは`anyhow`（呼び出し元でハンドリング不要なエラーの集約）、ライブラリコードは`thiserror`（呼び出し元が分岐できる型付きエラー）を使い分ける
- `panic!`はプログラミングエラー（回復不能な不変条件違反）のみに限定し、実行時に起こりうる異常（外部入力・I/O失敗等）には使わない
- エラーメッセージは握りつぶさず、元エラーを`source()`チェーンで保持する（`anyhow::Context` / `thiserror`の`#[from]`を使う）

## 3. 型システム・トレイト

- `impl Trait` vs `dyn Trait`（ジェネリクスによる静的ディスパッチ vs トレイトオブジェクトによる動的ディスパッチ）はバイナリサイズ・コンパイル時間・実行時オーバーヘッドのトレードオフで選ぶ
- ニュータイプパターン（`struct UserId(u64)`等）でプリミティブ型の意味の取り違えを防ぐ
- `From`/`TryFrom`で型変換を明示し、`as`によるキャストは数値の切り捨て・符号変換等意図が明確な場合のみ使う
- `derive`マクロ（`Debug`/`Clone`/`PartialEq`等）で実装できるものは手動実装しない

## 4. 非同期処理

- ランタイム（`tokio`/`async-std`）はプロジェクト内で統一する。混在させるとエグゼキュータの二重起動や`spawn`の失敗につながる
- `async`関数内で`std::sync::Mutex`等の同期ロックを保持したまま`.await`を跨がない（デッドロック・非効率なブロッキングの原因）。非同期対応版（`tokio::sync::Mutex`等）を使うか、ロックのスコープを`.await`前に閉じる
- CPUバウンドな重い処理を`async`タスク内で直接実行しない。`spawn_blocking`等の専用APIに逃がしてエグゼキュータのスレッドを塞がない
- `tokio::select!`でのキャンセル安全性（途中でタスクが中断されても状態が壊れないか）を確認する

## 5. unsafe

- `unsafe`ブロックには、なぜ安全性が保証されるかを説明するコメント（`// SAFETY: ...`）を必須とする
- `unsafe`の使用範囲は最小限にし、安全なAPIでラップして呼び出し側からは`unsafe`が見えないようにする
- FFI（`extern "C"`）境界では、Rust側とC側でメモリ所有権の受け渡しルール（誰が確保し誰が解放するか）を明文化する

## 6. Cargo・依存関係管理

- `Cargo.lock`はアプリケーションではコミットし、ライブラリ（crateとして配布するもの）ではコミットしない方針を明確にする
- 依存クレート追加時は`cargo tree`でバージョン重複・依存グラフの肥大化を確認する
- featureフラグの取り扱い（デフォルトfeatureの範囲、依存先crateのfeature unification）を意識し、不要な機能のコンパイルを避ける
- `cargo audit`（既知の脆弱性）・`cargo outdated`（更新可能な依存）を定期的に確認する

## 7. テスト

- ユニットテストは対象モジュール内`#[cfg(test)] mod tests`、統合テストは`tests/`ディレクトリに配置する使い分けを踏襲する
- モック化しづらい外部依存（DB・HTTP等）はトレイトで抽象化し、テスト用のフェイク実装に差し替えられる設計にする
- プロパティベーステスト（`proptest`等）を、境界値・不変条件の検証が重要なロジック（パーサー・数値計算等）に適用する

## 8. リンティング・フォーマット

- `cargo clippy`を警告ゼロで通す。`#[allow(clippy::...)]`で抑制する場合は理由をコメントで明記する
- `cargo fmt`でフォーマットを統一する。フォーマット崩れとロジック変更を同じコミット/差分に混在させない
- `#[deny(warnings)]`をコード内に直接書かず、CI側の`cargo build`/`clippy`オプションで制御する（ローカルビルドが将来の警告で壊れるのを防ぐ）

## 9. パフォーマンス

- ホットパスでの不要なヒープ確保（`String`/`Vec`の都度生成、`format!`の多用）を避け、可能な箇所は`&str`/スライスで済ませる
- イテレータチェーン（`.iter().map().collect()`等）はコンパイラが最適化しやすく、手書きループより優先して良いが、中間コレクションの生成回数には注意する
- release ビルド（`cargo build --release`）とdebugビルドで実行速度が大きく異なる点を踏まえ、パフォーマンス計測はreleaseビルドで行う
- `docs/audit-plan.md`のPerformanceカテゴリと同じ観点（N+1呼び出し・ループ内の重い処理・巨大データの全件取得）で監査する

## 10. ビルド・デプロイ

- クロスコンパイル対象（デプロイ先OS・CPUアーキテクチャ）が開発環境と異なる場合、`cross`等でビルド環境の差異を吸収する
- コンテナデプロイ（Cloud Run等）ではマルチステージビルドでビルド用イメージと実行用イメージを分離し、実行イメージのサイズ・攻撃対象を最小化する
- リリースバイナリに`RUST_BACKTRACE`等のデバッグ用環境変数がデフォルト有効になっていないか確認する
