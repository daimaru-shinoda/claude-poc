# プロジェクトデザイン設定

Web画面のUIについての編集方針を記述する

## 基本方針

- 対象ユーザー: 社員（社内システム）
- トンマナ: 落ち着いたイメージ
  - 彩度・明度を抑えた配色にする（原色・ビビッドカラーは避ける）
  - 装飾を最小限にし、余白で情報を整理する
  - アクセントカラーは強調が必要な箇所（ボタン、警告、グラフの注目系列）に限定して使う

## デザイントークン

### 配色（白基調・ブルー系）

| 用途                     | カラーコード                                   |
| ------------------------ | ---------------------------------------------- |
| 背景（メイン）           | `#FFFFFF`                                      |
| 背景（セクション区切り） | `#F5F7FA`                                      |
| プライマリ（ボタン等）   | `#4A6FA5`                                      |
| テキスト（本文）         | `#333333`                                      |
| 罫線・区切り             | `#E2E8F0`                                      |
| 警告・エラー             | 青系とは別系統（赤系など）を必要箇所のみに使用 |

- Why: 白基調は業務画面の圧迫感を抑え、トーンを落とした青は「落ち着いた」印象を保ちつつ視認性を確保できるため
- 純黒（`#000000`）や彩度の高い青（例: `#007BFF`）は使わない（コントラストが強すぎる／賑やかな印象になる）

### 余白（8pxグリッド）

| 用途 | サイズ |
|---|---|
| xs（要素内の詰め） | 4px |
| sm（要素間の基本間隔） | 8px |
| md（コンポーネント内の余白） | 16px |
| lg（セクション間） | 24px |
| xl（画面上下の余白等） | 32px |

- Why: 基準単位を決めておくことで、コンポーネントごとに余白がばらつくのを防ぎ、整った印象を保てるため

### フォントサイズ

| 用途 | サイズ |
|---|---|
| xs（補助ラベル、多用しない） | 12px |
| sm（キャプション等） | 14px |
| base（本文の基準） | 16px |
| lg（小見出し） | 20px |
| xl（大見出し） | 24px |

行間: `line-height: 1.6`（本文）を基準とする

- Why: 社内システムで利用者の年齢層・視力を考慮し、本文は16px未満にしない。行間も詰めすぎると可読性が下がるため1.5〜1.6を確保する
- 12px（xs）は補助ラベルのみに限定し、本文・操作系のテキストには使わない

### 実装方法

色・余白はハードコードせず、CSSカスタムプロパティとして定義し、実装側は変数を参照すること

```css
:root {
  --color-bg: #FFFFFF;
  --color-bg-section: #F5F7FA;
  --color-primary: #4A6FA5;
  --color-text: #333333;
  --color-border: #E2E8F0;

  --spacing-xs: 4px;
  --spacing-sm: 8px;
  --spacing-md: 16px;
  --spacing-lg: 24px;
  --spacing-xl: 32px;

  --font-size-xs: 12px;
  --font-size-sm: 14px;
  --font-size-base: 16px;
  --font-size-lg: 20px;
  --font-size-xl: 24px;
  --line-height-base: 1.6;
}
```

- Why: 変数化しておくことで、後からトーンや余白感を調整する際に定義箇所を1箇所変更するだけで済むため

## レイアウト

### チャートの日付軸

日付を横軸にするチャートを作成する場合は期間選択カレンダーを配置すること

- Why: 期間を絞れないと件数増加時にチャートが潰れて読めなくなるため

## フィードバック

### 一時メッセージ

画面に一時的に表示するメッセージ（○○を更新しました等）は直近のボタンの近くに配置すること

- Why: 更新箇所を視線移動なしで確認できるようにするため

### ポップアップ

ポップアップを表示する場合は画面中央上側に表示して目立たせること

- Why: 画面のどこにいても気づけるようにするため

## アンチパターン

### データ表示

- フィルタ・期間指定なしで全件をテーブル表示しない
  - Why: 件数増加時に読み込みが重くなり、目的の行も探しにくくなるため
- 色だけで意味（正常/異常など）を伝えない
  - Why: 色覚特性やモノクロ印刷で判別できなくなるため
- 折れ線・棒グラフの系列色を無闇に増やさない（目安5〜6色まで）
  - Why: 色数が増えると凡例と照合しないと判別できなくなるため

### 操作・フィードバック

- 削除・上書きなど元に戻せない操作を確認なしで即実行しない
  - Why: 誤操作によるデータ消失を防ぐため
- 通信中の画面を無反応のまま放置しない（ローディング表示を出す）
  - Why: フリーズと誤認され、二重クリックなどの誤操作を誘発するため
- エラー発生時に無言で処理を止めない（原因が分かるメッセージを出す）
  - Why: ユーザーが次に何をすべきか判断できなくなるため

### レイアウト

- モーダル/ポップアップを多重に重ねて表示しない
  - Why: 背後の情報が見えなくなり、閉じる操作も分かりにくくなるため
- 固定pxで画面幅を決め打ちしない（ウィンドウ幅の変化を考慮する）
  - Why: 業務用途でも画面サイズは統一されておらず、崩れの原因になるため
